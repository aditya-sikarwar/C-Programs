#include<stdio.h>
int main ()
{
    int radius;
    float area, perimete;
radius = 6;

perimeter=2*3.14*radius;
printf("The perimete of the given circle is %f\n,perimeter);

area= 3.14*radius*radius;
printf("Area of the Circle = %f square inches\n", area);

return (0);
}